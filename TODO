# Add the notification bell ✔
# Add a device registration form with attributes such as: % volume(progress bar), ID, 
# Send notification to the system when a certain percentage volume is reached.
# Add device field in the customers collection
